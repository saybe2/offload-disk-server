apiVersion: 1

providers:
  - name: Offload
    orgId: 1
    folder: Offload
    type: file
    disableDeletion: false
    allowUiUpdates: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards

